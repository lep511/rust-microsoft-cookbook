[package]
name = "langchain-app"
version = "0.1.0"
edition = "2021"

[dependencies]
langchain-rust = "4.6.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

tokio = { version = "1", features = ["full"] }
